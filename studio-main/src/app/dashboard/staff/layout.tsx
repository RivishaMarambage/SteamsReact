// This is a placeholder file to ensure the route is created.
// You can add a specific layout for the staff section here if needed.
export default function StaffLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
