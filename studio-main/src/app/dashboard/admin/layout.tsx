// This is a placeholder file to ensure the route is created.
// You can add a specific layout for the admin section here if needed.
export default function AdminLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
